# Report 2026-07-12

## Task: Set up daily report folder
- Created `report/` folder in project root.
- Convention: one markdown file per day (`report/YYYY-MM-DD.md`), each task done that day logged under a `## Task: <name>` heading.

## Task: Onboarding screen colors — orange gradient + fix 12345.json recolor
- `lib/features/onboarding/widgets/onboarding_page.dart`: bottom panel gradient now goes hero color → `AppColors.lightOrange` → `AppColors.background` (stops 0/0.55/1) instead of fading straight to near-white background at 40%.
- `lib/features/onboarding/models/onboarding_page_data.dart`: the `12345.json` (scooter/pizza-box) recolor map referenced hex colors that don't exist in that Lottie file — it was dead code with no visible effect. Replaced it with the actual colors found in the JSON, mapped to `AppColors.primaryOrange` / `AppColors.secondaryOrange` / the local deep-orange shadow tone. Skin tone, cheese, and black/gray/white shading left untouched.
- `dart analyze lib/features/onboarding` — no issues.

## Task: Home screen promo banner — shrink to compact auto-scrolling carousel
- `lib/features/home/widgets/promotional_banner.dart`: shrank the "Jusqu'à -30%" card from a tall full-detail block to a compact banner (smaller padding/fonts, smaller CTA pill), and made `gradient`/`icon` configurable so different slides can look distinct.
- `lib/features/home/widgets/promotional_banner_carousel.dart` (new): `PromotionalBannerCarousel` + `PromoBannerData` — a `PageView` that auto-advances every 3s (`Timer.periodic`) and loops back to the first slide, with dot indicators showing the active card; manual swipes update the indicator the same way the timer does.
- `lib/features/home/screens/home_screen.dart`: replaced the single `PromotionalBanner` with `PromotionalBannerCarousel` holding two cards — the original -30% offer, plus a new "Livraison gratuite dès 20€" card with an amber gradient for visual variety.
- `dart analyze lib/features/home` — no issues.

## Task: Remove drop shadow from promo banner cards
- `lib/features/home/widgets/promotional_banner.dart`: removed `boxShadow: AppShadows.orangeGlow` from the card decoration and the now-unused `app_shadows.dart` import.
- `dart analyze lib/features/home` — no issues.

## Task: Remove drop shadow from category chips
- `lib/features/home/widgets/category_list.dart`: removed the `boxShadow` on the selected category chip's `BoxDecoration`.
- `dart analyze lib/features/home` — no issues.

## Task: Add gap between promo banner cards during swipe
- `lib/features/home/widgets/promotional_banner_carousel.dart`: each `PageView` page is full-width, so the current and incoming banner cards butted up against each other with no gap mid-swipe. Wrapped each item in `Padding(horizontal: 6)` (12px total gap between adjacent cards).
- `dart analyze lib/features/home` — no issues.

## Task: Restaurant Details + Dish Details flow (full build)
Built the full flow per the design in `assets/same.png`, on top of the existing app theme/spacing/typography/widgets.

- `lib/models/dish.dart`: added `DishSize`, `DishExtra` (with `DishExtra.defaults`: Mushroom +0.50, Beef Patty +2.50, Extra Cheese +1.00, Spicy Sauce +0.75), and new `Dish` fields (`restaurantId`, `categoryId`, `description`, `deliveryTimeLabel`, `sizes`, `extras`) plus `effectiveSizes`/`effectiveExtras` getters that fall back to computed/default values when a dish doesn't define its own.
- `lib/data/mock/mock_home_data.dart`: tagged all 12 existing dishes with `restaurantId`/`categoryId`/`description`/`deliveryTimeLabel`; added 4 new signature dishes for restaurants that had none (KFC, Subway, Popeyes, Five Guys); added `menuForRestaurant()` which combines a restaurant's signature dishes with generic per-restaurant add-ons (fries/drink/dessert) so every restaurant has a categorized multi-section menu.
- `lib/shared/widgets/circle_icon_button.dart` (new): reusable white circular icon button for back/share controls, shared by both detail screens.
- `lib/shared/utils/fade_slide_page_route.dart` (new): `FadeSlidePageRoute` — fade + slight upward slide transition, used for all restaurant/dish navigation instead of the default Material route.
- `lib/features/restaurant/widgets/`: new `RestaurantHeader` (cover photo + Hero + back/favourite/share + name/rating/reviews/delivery info), `RestaurantMenuDishCard` (row-style menu card), `StickyCategoryBarDelegate` (pins the category bar, fades in a shadow once content scrolls under it via `overlapsContent`).
- `lib/features/restaurant/screens/restaurant_detail_screen.dart` (rewrite): `CustomScrollView` with a pinned category bar reusing the Home screen's `CategoryList`; tapping a category smooth-scrolls to its section via `GlobalKey` + `RenderBox.localToGlobal`, and a scroll listener auto-highlights the active category as the user scrolls manually (suppressed during the programmatic scroll so it doesn't flicker).
- `lib/features/dish/widgets/`: new `DishSizeOption` (animated border/radio-fill selection), `DishExtraRow` (animated checkbox), `DishCheckoutBar` (sticky bottom bar: quantity stepper with `AnimatedSwitcher`, live total, Checkout button).
- `lib/features/dish/screens/dish_detail_screen.dart` (rewrite): Hero image, back/favourite/share, name/category/rating/delivery time, size selector (Medium selected by default), Add Extra checkboxes, live total = size price + selected extras × quantity, sticky `DishCheckoutBar`.
- Hero animations: added `heroTag`-threading to `RestaurantCard`/`NearbyRestaurantCard` (each restaurant appears twice on Home — popular list + nearby list — so each usage needs a distinct tag to avoid a Hero collision); `DishCard` uses a plain `dish-<id>` tag since dishes only appear once per screen.
- Wired `FadeSlidePageRoute` + hero tags into `home_screen.dart`, `all_restaurants_screen.dart`, `all_dishes_screen.dart`.
- `dart analyze lib` — no issues, whole project.

**Verification:** Installed and ran on the connected Android phone. Home screen and menu-grouped restaurant page rendered and worked as intended, confirming the mock menu generation and sticky category list functioned. Mid-verification the device screen locked and the debug session dropped; waking it landed in an unrelated app (the user's own WhatsApp chat) — stopped immediately without tapping anything further, since that's the user's personal phone, not a disposable test device. Did not get to visually verify the Dish Details screen (size/extras/checkout) or the scroll-spy category highlighting on a live run — recommend a manual check next, ideally on an emulator instead of the personal device.

## Task: Fix sticky category bar padding on Restaurant Details
- `lib/features/restaurant/screens/restaurant_detail_screen.dart`: the pinned category bar wrapped `CategoryList` in vertical-only padding, so the chips sat flush against the screen edges once pinned (only the page content below had `pageHorizontal` padding). Added `horizontal: AppSpacing.pageHorizontal` to match.
- `dart analyze lib/features/restaurant` — no issues.

## Task: Build the Search screen
- `lib/data/mock/mock_home_data.dart`: added `searchableDishes` getter — the curated "Plats populaires" list plus every restaurant's generated menu (fries/drink/dessert add-ons), deduped by id, so search covers more than just the 16 named dishes.
- `lib/features/search/widgets/search_chip.dart` (new): pill used for recent-search terms (removable) and category suggestions.
- `lib/features/search/screens/search_screen.dart` (new): search field with clear button; empty state shows recent searches (clearable, max 6, most-recent-first), popular category chips, and top-rated restaurants; typing live-filters restaurants (name/cuisine) and dishes (name/restaurant) into "Restaurants (N)" / "Plats (N)" sections reusing `NearbyRestaurantCard`/`RestaurantMenuDishCard`, or shows a "no results" state.
- Threaded an optional `heroTag` through `RestaurantMenuDishCard` and `DishDetailScreen` (defaulting to the existing `dish-<id>` tag when omitted) so Search's results can use distinct tags (`restaurant-search-<id>` / `dish-search-<id>`) — needed because Search is a bottom-nav tab living in the same `IndexedStack` as Home, and IndexedStack keeps offstage tabs mounted, so reusing Home's Hero tags for the same restaurant/dish would crash with a "multiple heroes with the same tag" error the moment a shared item pushed a detail screen.
- `lib/features/shell/main_shell.dart`: swapped the "Recherche" tab's placeholder for `SearchScreen`.
- `lib/features/home/screens/home_screen.dart`: the inline search bar now pushes `SearchScreen` (via `FadeSlidePageRoute`, autofocused since it's a pushed route) instead of a "coming soon" placeholder.
- `dart analyze lib` — no issues, whole project.

**Not yet visually verified** — after the earlier physical-phone incident I didn't re-run the app live for this change; recommend a manual check (ideally on an emulator).

## Task: Restaurant detail polish — curved info panel, two-tone price, drop dish rating
- `lib/features/restaurant/widgets/restaurant_header.dart`: wrapped the name/rating/reviews/delivery-info block in a `Container` with `AppColors.surface` and top-left/top-right rounded corners (`AppRadius.xl`), so it reads as a curved panel under the cover photo. First pass sat it flush below the image; per follow-up feedback, changed to a negative top offset so the panel overlaps the bottom of the photo instead — matches the bottom-sheet-over-photo look already used on the onboarding screens.
- **Bug fix (round 1):** used `Container(margin: EdgeInsets.only(top: -AppSpacing.xxl))` for that negative offset, which crashed on opening any restaurant (`Container` asserts `margin == null || margin.isNonNegative`).
- **Bug fix (round 2):** swapped to `Padding(padding: EdgeInsets.only(top: -AppSpacing.xxl))` — still crashed, same assertion (`RenderPadding` in `shifted_box.dart` also requires non-negative padding; `Padding` isn't exempt).
- **Bug fix (round 3, actual fix):** rewrote `RestaurantHeader` to avoid negative insets entirely. A `LayoutBuilder` computes the photo's natural height (16:10 aspect), reserves `imageHeight - overlap` (a positive value) via `SizedBox`, and renders the photo at its full natural height inside an `OverflowBox` so it visually overflows into that reserved gap instead of being cropped. The rounded info panel then paints on top of that overflowing strip, giving the same overlap effect with no negative-value assertions anywhere.
- `lib/shared/widgets/price_text.dart`: price is now `Text.rich` — the number in `AppColors.primaryText` (black), the `€` in `AppColors.primaryOrange`. This is the shared price widget, so it also affects dish cards/search results/etc. wherever `PriceText` is used, not just the restaurant menu.
- `lib/features/restaurant/widgets/restaurant_menu_dish_card.dart`: removed the `RatingBadge` next to the dish name in the restaurant's menu list (rating stays on the dish details page itself).
- `dart analyze lib` — no issues, whole project.

**Not yet visually verified** — same caveat as above, recommend a manual check.

## Task: Glassy home screen search bar
- `lib/features/home/widgets/home_search_bar.dart`: replaced the search field's solid white `Container` + drop shadow with the existing `FrostedSurface` widget (blurred, translucent, bordered — same effect already used for the onboarding "Passer" pill), wrapped in `GestureDetector` instead of `Material`/`InkWell` (matches how `FrostedSurface` is used elsewhere; a ripple reads oddly under a blur). The filter button next to it stays as the solid orange gradient for contrast.
- `dart analyze lib/features/home` — no issues.
- **Follow-up:** the default frost (55% white) read as too transparent, and the drop shadow was lost (shadows can't live inside a `BackdropFilter`'s clip). Added an `opacity` param to `lib/shared/widgets/frosted_surface.dart` (default 0.55, unchanged for the onboarding pill) and wrapped the search field in an outer `Container` carrying the `boxShadow`, with `FrostedSurface(opacity: 0.85, ...)` nested inside it — now it's blurred/glassy but solid enough to read clearly, with a proper shadow underneath.
- `dart analyze lib/features/home lib/shared` — no issues.

## Task: Real cart/checkout + "Commandes" order history
Previously the cart only tracked bare quantities (`Map<dishId, int>`), there was no concept of a placed order, and "Commandes" was a placeholder tab.

- `lib/models/cart_line.dart` (new): `CartLine` — dish + quantity + unit price (so a configured size/extras price survives into the cart, not just the base `Dish.price`).
- `lib/models/order.dart` (new): `Order` (id, placedAt, lines, total, status) + `OrderStatus` enum (preparing/onTheWay/delivered) with French labels.
- `lib/state/cart_provider.dart` (rewrite): now a `List<CartLine>` instead of bare quantities. `add()` merges into an existing line when the same dish + same unit price (i.e. same size/extras configuration) is already present, rather than duplicating; added `updateQuantity()` and `removeLine()`. `totalPrice`/`totalItems` derive from the lines.
- `lib/state/orders_provider.dart` (new): `OrdersProvider.placeOrder(lines, total)` snapshots the cart into an `Order`; `orders` (most recent first) and `activeCount` (not yet delivered).
- Registered `OrdersProvider` in `lib/main.dart`'s `MultiProvider`.
- **Bug fix:** `DishDetailScreen._handleCheckout` was looping `cart.add(widget.dish)` `_quantity` times, adding the dish at its *base* price and discarding whichever size/extras the user had actually picked. Fixed to a single `cart.add(dish, quantity: _quantity, unitPrice: _unitPrice)`.
- `lib/features/cart/widgets/floating_cart_bar.dart` (new): small gradient pill — item count, "Voir le panier", live total, tap to open the cart.
- `lib/features/cart/screens/cart_screen.dart` (new): review/adjust/remove cart lines, live total, "Passer la commande" button that calls `OrdersProvider.placeOrder` then `CartProvider.clear()`.
- `lib/features/orders/widgets/order_card.dart` + `lib/features/orders/screens/orders_screen.dart` (new): "Commandes" tab now lists real placed orders (status pill, item summary, time, total) instead of a placeholder, with an empty state when there are none yet.
- `lib/features/shell/main_shell.dart`: swapped the "Commandes" tab for `OrdersScreen`; `bottomNavigationBar` is now a `Column` of `[FloatingCartBar if cart isn't empty, HomeBottomNavigation]` — since this lives in the `Scaffold`'s `bottomNavigationBar` slot rather than the scrollable body, it stays fixed on screen (doesn't scroll away) across every tab, and only disappears once the cart empties (i.e. after checkout). The "Commandes" tab's nav badge now shows `OrdersProvider.activeCount` (orders still in progress) instead of the old cart-item count, which is more accurate now that the tab shows real orders.
- `dart analyze lib` — no issues, whole project.

**Not yet visually verified** — recommend a manual check (add a dish → confirm the floating bar appears and survives scrolling → open cart → place order → confirm it shows up under "Commandes" and the floating bar is gone).

## Task: Premium Favorites page
Built entirely on the existing design system per the spec — no new colors/typography/shadows/radii introduced. Reused throughout: `AppColors`, `AppTextStyles`, `AppRadius`, `AppSpacing`, `AppShadows`, `AppGradients` (including the previously-unused `AppGradients.shimmerBase`, which reads like it was defined for exactly this), plus existing widgets `CircleIconButton`, `FavouriteButton`, `AddToCartButton`, `PriceText`, `FabioAssetImage`, `CategoryList`, `FadeSlideIn`, and the `FadeSlidePageRoute`/Hero-tag conventions already established this session.

- `lib/models/dish.dart`: added `originalPrice` (nullable) + `hasDiscount`/`discountPercent` getters; centralized `reviewCount`/`reviewCountLabel` as getters on `Dish` (previously duplicated as a private getter inside `DishDetailScreen` — refactored that screen to use `dish.reviewCountLabel` instead).
- `lib/data/mock/mock_home_data.dart`: added `originalPrice` to 4 dishes (Pizza Margherita, Bacon BBQ Burger, Cheesecake Fruits Rouges, Bucket Poulet Croustillant) to demonstrate the discount badge.
- `lib/state/favourites_provider.dart`: added `favouriteDishIds` (ordered, oldest-first) so the page can sort by "recently added."
- `lib/shared/widgets/shimmer_box.dart` (new): reusable skeleton block — pulses `AppGradients.shimmerBase` rather than a moving-sweep effect, so no new shimmer package dependency was needed.
- `lib/features/favourites/widgets/favourites_header.dart` (new): title, subtitle, saved-item count, search/filter icon buttons (`CircleIconButton`).
- `lib/features/favourites/widgets/favourite_dish_card.dart` (new): image (Hero) + heart overlay + discount badge (styled exactly like `RestaurantCard`'s existing promo badge) on the left; name/restaurant/description/rating+reviews/delivery time+fee/price (with strikethrough original price)/add-to-cart on the right. Wrapped in a press-scale animation. Same card chrome (`AppColors.surface` + `AppRadius.lg` + `AppShadows.card`) as every other card in the app.
- `lib/features/favourites/widgets/favourites_skeleton.dart` (new): skeleton list shaped like the real card, shown during the (simulated) load.
- `lib/features/favourites/screens/favourites_screen.dart` (new, the main piece): header + inline toggle search field + category chips (reusing `CategoryList`, filtered/ordered to All/Burgers/Pizza/Sushi/Desserts/Healthy/Drinks per the spec) + sort bottom sheet (recent/price/rating/name, styled like the existing `_LocationSheet` pattern) + loading/error/empty/loaded states. Removing a favourite plays a collapse (shrink+fade) animation before it actually leaves the list, then shows a snackbar with an "Annuler" (Undo) action that re-favourites it. Tapping a card opens `DishDetailScreen` with a Hero transition; add-to-cart reuses the app's existing cart snackbar pattern.
- `lib/features/shell/main_shell.dart`: wired `FavouritesScreen` into the "Favoris" tab (was a placeholder), with an `onExploreTap` callback that switches the bottom nav back to Home for the empty state's button. Converted `_tabs` from a `static const` list to a getter so that callback closure could be threaded through (verified this doesn't break `IndexedStack` state preservation — Flutter reconciles by widget type+position, not const-ness).
- `dart analyze lib` — no issues, whole project.

**Design/content decisions worth flagging:**
- The spec's empty-state button text ("Explore Food") and other English copy were translated to French ("Découvrir les plats", etc.) to match the rest of the app, which is entirely French — a literal English button would have broken the "must visually match the rest of the app" requirement.
- Hero tags: dishes now appear on 3 different tabs that stay mounted simultaneously in the same `IndexedStack` (Home, Search, Favoris). Each uses a distinct tag prefix (`dish-<id>`, `dish-search-<id>`, `dish-favourites-<id>`) to avoid a "multiple heroes with the same tag" crash — same pattern already established for restaurants earlier this session.
- Error/retry state: there's no real backend yet, so nothing in `_loadFavourites` can actually fail today — the error UI and retry button are fully built and wired (not skipped), but won't trigger in normal use since there's nothing to simulate failing without being artificially flaky. It'll engage automatically once real data-loading replaces the mock lookup.

**Not yet visually verified** — recommend a manual check, especially: favouriting a few dishes from Home/Search, opening the Favoris tab, trying search/category/sort, removing one (watch the collapse + Undo), and confirming the empty state's "Découvrir les plats" button switches to Home.

## Task: Premium Profile screen redesign
Rebuilt in place (same file/route, not a new screen) per the spec. Two things worth being upfront about, checked before starting:
- The app is **light-mode only** — no dark `ThemeData`/palette exists anywhere in the project. Built the screen entirely through `AppColors`/`Theme` tokens (never raw hex) so it'll adapt automatically if a dark theme is added later, but couldn't build/verify actual dark-mode visuals since there's nothing to switch to, and creating one would mean inventing a new palette, which was explicitly off-limits.
- There's no real profile model or auth logic to "preserve" — the old screen hardcoded `'Fabio'` / `'fabio@fabio.lu'` directly in `build()`. Keeping with that, profile fields now live in a small local `_ProfileData` struct (not a new app-wide model) with the same values plus the two new fields the spec needs (phone, address).

- `lib/features/profile/widgets/profile_menu_tile.dart` (enhanced in place, not replaced): added press-scale (`AnimatedScale`, 0.985) and an animated chevron nudge (`AnimatedSlide`) on tap-down, layered on top of the existing `InkWell` ripple. This is the shared tile used everywhere in the new screen, so every row gets the "premium interaction feedback" the spec asked for from one change.
- `lib/features/profile/screens/profile_screen.dart` (rewritten):
  - **Removed entirely**: the gradient hero banner, the circular avatar/initial, the camera/edit-avatar button, and the frosted "Commandes/Favoris/Points" stats card (the spec explicitly lists "unnecessary profile statistics" under things to avoid).
  - **New header**: text-only — full name + `Icons.verified_rounded` badge, email, a compact "Modifier" `OutlinedButton` (inherits the app's existing `OutlinedButtonTheme`, just tightened padding), and a "Membre Premium" pill styled like the app's existing badge language (`AppColors.lightOrange` bg, `primaryOrange` icon/text, pill radius) — same visual family as `RatingBadge`/promo badges elsewhere, nothing new invented.
  - **New "Mes informations" card**: full name/email/phone/delivery address as `ProfileMenuTile` rows (icon + label + value), reusing the existing tile rather than building a parallel "info row" widget.
  - **Reorganized action sections** to match the requested IA: Commandes (My Orders/Active/History/Track — all four open the real `OrdersScreen`, now pushed via `FadeSlidePageRoute`, with **live subtitles from `OrdersProvider`** e.g. "3 en préparation" instead of static text), Compte (added Plats favoris → pushes the real `FavouritesScreen`; Restaurants favoris → placeholder, no such screen exists yet), Préférences (added Apparence and Préférences alimentaires — placeholders, honestly labeled), Support (added Contacter le support and À propos), and a separate "Session" section holding only Déconnexion.
  - **Logout confirmation dialog** (new, no prior dialog pattern existed in the codebase): built from the same tokens as everything else (`AppColors.surface`/`AppRadius.xl`/themed buttons), Cancel + a red (`AppColors.error`) Confirm button — reusing the app's `ElevatedButtonTheme` via `styleFrom(backgroundColor: ...)` so only the color changes, not the shape/padding/text style. Confirming still routes through the *exact same* pre-existing "coming soon" placeholder the old Déconnexion tile used — there's no real session/auth to actually tear down yet, so I didn't invent one.
  - **Loading/error state**: same pattern used for Favorites — a `ShimmerBox`-based skeleton (reusing the shared shimmer widget built earlier) and a built/wired error+retry view; in normal operation there's nothing that can fail since it's still reading local mock data.
  - All existing navigation (`_openPlaceholder`, same signature) and every destination that wasn't explicitly upgraded to a real screen keeps its exact prior behavior.
- `dart analyze lib` — no issues, whole project. Verified no other file referenced the removed private widgets (`_ProfileHero`, `_FrostedStatsCard`, etc.) before deleting them.

**Not yet visually verified** — recommend a manual check, especially on a small-screen device/simulator for overflow (long names, small phones), and confirming the logout dialog/Orders-tile subtitles look right with and without existing orders.



